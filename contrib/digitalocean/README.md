### Usage

```
cp terraform.tfvars.example  terraform.tfvars
curl "https://discovery.etcd.io/new?size=3" 
curl -X GET -H 'Content-Type: application/json' -H 'Authorization: Bearer ${your_digitalocean_token}' "https://api.digitalocean.com/v2/account/keys"
```

